# ============================================================
# MRMPFormer 依赖配置 (Python 3.10 ~ 3.11)
# ============================================================
# MRMPFormer 是基于 SimCLR 的色谱峰 ROI 图像对比学习模型。
# 核心：ResNet50 backbone + MLP projection head + NT-Xent loss。
# 安装方式:
#   根据你的 GPU 型号, 取消/注释对应 PyTorch 段, 然后:
#   pip install -r requirements.txt
# ============================================================

# --- 核心数值与数据处理 ---
numpy==1.26.4

# --- 图像加载与处理 ---
Pillow==10.4.0

# --- 进度条 ---
tqdm==4.68.4

# --- TensorBoard 训练日志 (SummaryWriter) ---
tensorboard>=2.14.0

# ============================================================
# PyTorch — 根据你的 GPU 型号, 只取消 **一组** 配置的注释
# ============================================================
# 使用说明:
#   1. 找到你 GPU 对应的配置段
#   2. 取消该段内 --index-url / torch / torchvision 的注释
#   3. 确保其他 GPU 段的配置行保持注释状态
# ============================================================

# ----------------------------------------------------------
# CPU Only — 无 NVIDIA GPU (Intel / AMD / Apple Silicon)
# ----------------------------------------------------------
# --index-url https://download.pytorch.org/whl/cpu
# torch==2.6.0
# torchvision==0.21.0

# ----------------------------------------------------------
# NVIDIA RTX 50 系列 (Blackwell: 5060 / 5070 / 5080 / 5090)
#   CUDA 12.8 | 计算能力 12.x | ⬇ 当前本机配置 (RTX 5060)
# ----------------------------------------------------------
--index-url https://download.pytorch.org/whl/cu128
torch==2.7.0
torchvision==0.22.0

# ----------------------------------------------------------
# NVIDIA RTX 40 系列 (Ada Lovelace: 4060 / 4070 / 4080 / 4090)
#   CUDA 12.4 | 计算能力 8.9
# ----------------------------------------------------------
# --index-url https://download.pytorch.org/whl/cu124
# torch==2.6.0
# torchvision==0.21.0

# ----------------------------------------------------------
# NVIDIA RTX 30 系列 (Ampere: 3050 / 3060 / 3070 / 3080 / 3090)
#   CUDA 12.4 | 计算能力 8.6
# ----------------------------------------------------------
# --index-url https://download.pytorch.org/whl/cu124
# torch==2.6.0
# torchvision==0.21.0

# ----------------------------------------------------------
# NVIDIA RTX 20 系列 / GTX 16 系列 (Turing)
#   CUDA 12.4 | 计算能力 7.5
# ----------------------------------------------------------
# --index-url https://download.pytorch.org/whl/cu124
# torch==2.6.0
# torchvision==0.21.0
