# ============================================================
# QuanFormer 统一依赖配置 (Python 3.10 ~ 3.11)
# ============================================================
# 安装方式:
#   根据你的 GPU 型号, 取消/注释对应 PyTorch 段, 然后:
#   pip install -r requirements.txt
# ============================================================

# --- 核心数值与数据处理 ---
numpy==1.26.4
pandas==2.2.2
scipy==1.13.1

# --- 质谱数据解析 ---
pymzml==2.5.10

# --- 并行与工具 ---
joblib==1.4.2
natsort==8.4.0
packaging>=24.0

# --- 可视化 ---
matplotlib==3.9.2
seaborn>=0.13.0

# --- GUI ---
pyside6>=6.7.0,<6.8.0

# --- 图像处理 ---
pillow>=10.0.0,<11.0.0
pycocotools>=2.0.0

# ============================================================
# PyTorch — 根据你的 GPU 型号, 只取消 **一组** 配置的注释
# ============================================================
# 使用说明:
#   1. 找到你 GPU 对应的配置段
#   2. 取消该段内 --index-url / torch / torchvision 的注释
#   3. 确保其他 GPU 段的配置行保持注释状态
# ============================================================

# ----------------------------------------------------------
# CPU Only — 无 NVIDIA GPU (Intel / AMD / Apple Silicon)
# ----------------------------------------------------------
# --index-url https://download.pytorch.org/whl/cpu
# torch==2.6.0
# torchvision==0.21.0

# ----------------------------------------------------------
# NVIDIA RTX 50 系列 (Blackwell: 5060 / 5070 / 5080 / 5090)
#   CUDA 12.8 | 计算能力 12.x
# ----------------------------------------------------------
# --index-url https://download.pytorch.org/whl/cu128
# torch==2.7.0
# torchvision==0.22.0

# ----------------------------------------------------------
# NVIDIA RTX 40 系列 (Ada Lovelace: 4060 / 4070 / 4080 / 4090)
#   CUDA 12.4 | 计算能力 8.9 | ⬇ 当前本机配置 (4090D)
# ----------------------------------------------------------
--index-url https://download.pytorch.org/whl/cu124
torch==2.6.0
torchvision==0.21.0

# ----------------------------------------------------------
# NVIDIA RTX 30 系列 (Ampere: 3050 / 3060 / 3070 / 3080 / 3090)
#   CUDA 12.4 | 计算能力 8.6
# ----------------------------------------------------------
# --index-url https://download.pytorch.org/whl/cu124
# torch==2.6.0
# torchvision==0.21.0

# ----------------------------------------------------------
# NVIDIA RTX 20 系列 / GTX 16 系列 (Turing)
#   CUDA 12.4 | 计算能力 7.5
# ----------------------------------------------------------
# --index-url https://download.pytorch.org/whl/cu124
# torch==2.6.0
# torchvision==0.21.0

# --- 可选: workbook 脚本 ---
# openpyxl>=3.0.0
# scikit-learn>=1.3.0
# statsmodels>=0.14.0